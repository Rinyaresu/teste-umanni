<%= form_with(model: [post, post.comments.build]) do |form| %>
  <div>
    <%= form.rich_text_area :content %>
    <%= form.submit 'Reply', class: 'mt-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>
  </div>
<% end %>
